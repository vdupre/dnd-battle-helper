import type { NextPage } from "next";
import { Layout } from "../../components/layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <h2>Battle</h2>
    </Layout>
  );
};

export default Home;
